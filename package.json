{
  "name": "super-admin",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "build": "pnpm --filter server build && pnpm --filter client build",
    "test:server": "pnpm --filter server run test --runInBand",
    "test:extension": "node --test extension/*.test.js",
    "test": "pnpm run test:server && pnpm run test:extension",
    "db:validate": "pnpm --filter server exec prisma validate --schema prisma/schema.prisma",
    "db:generate": "pnpm --filter server exec prisma generate --schema prisma/schema.prisma"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "@nestjs/core",
      "msgpackr-extract",
      "unrs-resolver"
    ]
  }
}
